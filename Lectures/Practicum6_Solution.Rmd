---
title: "Practicum 6"
output:
  pdf_document: default
  html_notebook: default
  html_document: default
---

Read in the data:

```{r cache=TRUE}
library(WHO)
infant <- get_data('EQ_INFANTMORT')
child <- get_data('EQ_U5MORT')
```

Look at the data

```{r}
summary(infant)
summary(child)
```

Find common columns:

```{r}
intersect(colnames(child), colnames(infant))
```
2. Include only rows with NAs in specified columns
```{r}
child <- child[is.na(child$wealthquintile) & is.na(child$residenceareatype),]
infant <- infant[is.na(infant$wealthquintile) & is.na(infant$residenceareatype),]
```
3. Omit some columns
```{r}
library(dplyr)
columnsToDrop <- c("residenceareatype", "wealthquintile", "gho", "publishstate")
child <- select(child, - one_of(columnsToDrop))
infant <- select(infant, - one_of(columnsToDrop))
```

What columns are still in common?

```{r}
intersect(colnames(child), colnames(infant))
```
